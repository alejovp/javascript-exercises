// Define a function called ​isString​ that receives a
// value and return true if the value received is a string

function isString (value) { // eslint-disable-line no-unused-vars
  return typeof value === 'string'
}
