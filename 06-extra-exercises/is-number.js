// Define a function called ​isNumber​ that receives a
// value and return true if the value received is a number

function isNumber (value) { // eslint-disable-line no-unused-vars
  return typeof value === 'number'
}
